apiVersion: gateway.networking.k8s.io/v1beta1
kind: HTTPRoute
metadata:
  name: flash-http-route
  namespace: flash5
spec:
  parentRefs:
  - name: flash-gateway
  hostnames:
  - "sclzs1.stellarengine.dev"
  rules:
  - backendRefs:
    - name: flash-service
      port: 80