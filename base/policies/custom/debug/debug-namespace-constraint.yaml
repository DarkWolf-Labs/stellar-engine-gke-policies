apiVersion: constraints.gatekeeper.sh/v1beta1
kind: DebugListNamespaces
metadata:
  name: debug-list-all-namespaces
spec:
  match:
    kinds:
      - apiGroups: [""]
        kinds: ["Namespace"]
    namespaces: ["default"]